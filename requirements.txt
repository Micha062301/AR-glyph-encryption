# Image capture and processing
opencv-python>=4.10.0
pillow>=10.0.0

# Core ML and model dependencies
torch>=2.10.0
torchvision>=0.25.0
accelerate>=1.13.0
sentencepiece>=0.2.0

# GLM-OCR requires transformers 5.12+ (install from git if PyPI build is too old)
transformers>=5.12.0
